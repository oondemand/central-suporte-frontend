apiVersion: v1
kind: Service
metadata:
  name: central-suporte-frontend-service
  namespace: central-suporte
spec:
  type: NodePort
  selector:
    app: central-suporte-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
