apiVersion: apps/v1
kind: Deployment
metadata:
  name: central-suporte-frontend-homolog
  namespace: central-suporte-homolog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: central-suporte-frontend-homolog
  template:
    metadata:
      labels:
        app: central-suporte-frontend-homolog
    spec:
      containers:
        - name: central-suporte-frontend-homolog-container
          image: ghcr.io/oondemand/central-suporte-frontend:${IMAGE_TAG}-homolog
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "64Mi"
              cpu: "0.05"
            limits:
              memory: "128Mi"
              cpu: "0.1"
